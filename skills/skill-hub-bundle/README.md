# Skill Hub全家桶

一键安装OpenClaw Skill Hub核心功能，让用户一句话就能获得完整的技能管理能力。

## 🎯 一句话安装

```bash
安装skillhub
```

## 功能特色

### 🚀 一键安装全家桶
- **一句话安装**: 用户只需说"安装skillhub"
- **自动配置**: 自动设置环境变量和路径
- **依赖检查**: 自动检查并安装必要依赖
- **进度显示**: 实时显示安装进度

### 📦 包含的核心功能

#### 1. **技能同步** (`skill-hub-sync`)
- 从Skill Hub同步最新技能
- 支持指定同步单个或多个技能
- 自动更新本地技能库

#### 2. **技能提交** (`skill-hub-submit`)
- 将本地技能提交到Skill Hub共享
- 技能验证和打包
- 支持手动和自动提交

#### 3. **增强版技能列表** (`skill-hub-list`)
- 分类、分页、搜索和过滤
- 解决技能列表过长问题
- 智能分类和统计信息

### 🔧 统一管理
- **统一命令**: `skillhub` 命令管理所有功能
- **状态查看**: `skillhub status` 查看安装状态
- **环境配置**: 自动配置shell环境
- **全局命令**: 可选的全局命令链接

## 快速开始

### 安装方法

#### 方法一：一句话安装（推荐）
```bash
安装skillhub
```

#### 方法二：带选项安装
```bash
安装skillhub --force          # 强制重新安装
安装skillhub --help           # 显示帮助
```

### 安装流程
1. **检查依赖** - 自动检查git、curl等
2. **下载技能** - 从GitHub下载核心技能
3. **配置环境** - 设置环境变量和路径
4. **创建命令** - 创建统一管理命令
5. **验证安装** - 检查安装是否成功

### 安装后命令

#### 查看技能
```bash
列出技能                    # 分页显示所有技能
列出技能 --category 工具    # 按分类查看
列出技能 --search 天气      # 搜索技能
列出技能 --stats            # 查看统计
```

#### 安装技能
```bash
同步技能 weather              # 安装天气技能
同步技能 daily-checkin-reminder  # 安装打卡提醒
同步技能 novel-rewrite-assistant # 安装小说仿写助手
```

#### 分享技能
```bash
提交技能 /path/to/your-skill  # 分享你的技能到Skill Hub
```

#### 管理Skill Hub
```bash
skillhub status              # 查看Skill Hub状态
skillhub help                # 显示帮助
```

## 文件结构
```
skill-hub-bundle/
├── SKILL.md                  # 技能文档
├── skillhub-bundle.sh        # OpenClaw技能包装器
├── install_complete.sh       # 完整安装脚本
├── README.md                 # 本文件
└── config/                   # 配置文件模板
```

## 配置说明

### 安装目录
默认安装到：`~/.openclaw/extensions/skill-hub/`

### 环境变量
安装后会自动添加以下环境变量：
```bash
export SKILL_HUB_PATH="$HOME/.openclaw/extensions/skill-hub"
export PATH="$PATH:$SKILL_HUB_PATH/skills/skill-hub-sync"
export PATH="$PATH:$SKILL_HUB_PATH/skills/skill-hub-submit"
export PATH="$PATH:$SKILL_HUB_PATH/skills/skill-hub-enhanced-list"
```

### 配置文件
`~/.openclaw/extensions/skill-hub/config.json`
```json
{
  "installation": {
    "version": "1.0.0",
    "installed_at": "2026-02-27T23:04:17Z",
    "components": ["sync", "submit", "list"],
    "auto_update": true
  },
  "core_skills": {
    "sync": "skill-hub-sync",
    "submit": "skill-hub-submit",
    "list": "skill-hub-enhanced-list"
  }
}
```

## 使用示例

### 完整工作流
```bash
# 1. 安装Skill Hub全家桶
安装skillhub

# 2. 查看可用技能
列出技能

# 3. 安装天气技能
同步技能 weather

# 4. 使用天气技能
天气 北京

# 5. 创建自己的技能
mkdir my-awesome-skill
# 添加SKILL.md等文件...

# 6. 分享到Skill Hub
提交技能 my-awesome-skill
```

### 高级用法
```bash
# 强制重新安装
安装skillhub --force

# 查看Skill Hub状态
skillhub status

# 批量安装技能
同步技能 weather daily-checkin-reminder novel-rewrite-assistant

# 搜索特定类型技能
列出技能 --category 工具
列出技能 --author 小怪
```

## 故障排除

### 常见问题

#### 1. 安装失败
```bash
# 检查网络连接
ping github.com

# 检查依赖
git --version
curl --version

# 手动运行安装脚本
bash install_complete.sh --force
```

#### 2. 命令找不到
```bash
# 重新加载shell配置
source ~/.bashrc  # 或 source ~/.zshrc

# 手动添加路径
export PATH="$PATH:$HOME/.openclaw/extensions/skill-hub/skills/skill-hub-sync"
```

#### 3. 技能同步失败
```bash
# 检查GitHub访问
curl -I https://github.com/guaidashu/openclaw-skill-hub

# 更新技能同步工具
同步技能 skill-hub-sync
```

#### 4. 权限问题
```bash
# 检查目录权限
ls -la ~/.openclaw/

# 修复权限
chmod +x ~/.openclaw/extensions/skill-hub/skills/*/*.sh
```

## 更新和维护

### 更新Skill Hub
```bash
# 更新核心技能
同步技能 skill-hub-sync
同步技能 skill-hub-submit
同步技能 skill-hub-enhanced-list

# 或重新安装
安装skillhub --force
```

### 卸载Skill Hub
```bash
# 手动卸载
rm -rf ~/.openclaw/extensions/skill-hub
# 从shell配置中移除相关行
```

### 备份和恢复
```bash
# 备份配置
cp -r ~/.openclaw/extensions/skill-hub ~/skillhub-backup

# 恢复配置
cp -r ~/skillhub-backup ~/.openclaw/extensions/skill-hub
```

## 扩展功能

### 自定义安装
```bash
# 指定安装目录
安装skillhub --path /opt/skillhub

# 精简安装（仅核心）
安装skillhub --lite
```

### 集成到OpenClaw
安装后，以下命令会自动注册到OpenClaw：
- `安装skillhub`
- `列出技能`
- `同步技能`
- `提交技能`

### 开发扩展
你可以基于此全家桶开发：
- 图形化安装界面
- 技能市场功能
- 自动更新系统
- 多用户支持

## 性能优化

### 安装优化
- **增量下载**: 只下载变化的文件
- **并行处理**: 同时安装多个组件
- **缓存利用**: 重用已下载的文件
- **断点续传**: 支持中断后继续

### 运行优化
- **懒加载**: 需要时才加载技能
- **内存缓存**: 缓存常用数据
- **异步操作**: 不阻塞主进程

## 安全考虑

### 安装安全
- 验证下载来源
- 检查文件完整性
- 限制安装权限
- 隔离运行环境

### 运行安全
- 技能权限控制
- 输入验证和过滤
- 资源使用限制
- 日志和审计

## 更新日志

### v1.0.0 (2026-02-27)
- 初始版本发布
- 一键安装全家桶功能
- 包含三个核心技能
- 统一管理命令
- 自动环境配置

## 许可证

MIT License - 详见 LICENSE 文件

## 支持与反馈

- **GitHub Issues**: 提交问题和建议
- **功能请求**: 描述需要的功能
- **错误报告**: 提供复现步骤和日志

---

**让Skill Hub安装更简单，让技能分享更便捷！** 🚀